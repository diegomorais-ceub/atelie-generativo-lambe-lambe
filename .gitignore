# Credenciais / segredos
.env
*.key
*token*

# Dados pesados / pesos de modelo (usar Hub/Drive)
*.safetensors
*.ckpt
*.bin
checkpoints/

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/

# Legendas / metadata e outros .txt/.jsonl gerados (rascunhos regeneráveis no notebook 01)
*.txt
*.jsonl
# ...exceto os arquivos que PRECISAM ficar versionados:
!**/requirements.txt      # dependências do Space (sem isso o app não sobe)
!dados/legendas.txt       # entregável: legendas revisadas (checklist §7)
!dados/metadata.jsonl     # insumo do treino, referenciado no README

# SO
.DS_Store

# LibreOffice lock files
.~lock.*#

