# Credenciais / segredos
.env
*.key
*token*

# Dados pesados / pesos de modelo (usar Hub/Drive)
*.safetensors
*.ckpt
*.bin
checkpoints/

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/

# Legendas / metadata e outros .txt/.jsonl — GERADOS na execução do notebook 01 (BLIP + revisão),
# portanto NÃO versionados. Reconstruídos a partir do fontes.csv + imagens.
*.txt
*.jsonl
!**/requirements.txt      # exceção: dependências do Space (sem isso o app não sobe)

# SO
.DS_Store

# LibreOffice lock files
.~lock.*#

