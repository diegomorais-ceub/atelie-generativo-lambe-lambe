# Credenciais / segredos
.env
*.key
*token*

# Dados pesados / pesos de modelo (usar Hub/Drive)
*.safetensors
*.ckpt
*.bin
checkpoints/

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/

# .txt/.jsonl são gerados (BLIP); o metadata.jsonl (insumo do treino) fica fora do versionamento.
# Exceções abaixo PRECISAM ficar versionadas (comentário em linha própria — .gitignore não aceita inline).
*.txt
*.jsonl
# dependências do Space (sem isso o app não sobe):
!**/requirements.txt
# legendas REVISADAS (entregável do checklist §7 / Critério 2):
!dados/legendas.txt

# SO
.DS_Store

# LibreOffice lock files
.~lock.*#

